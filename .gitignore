# Nix build output
result
result-*

# direnv
.direnv/

# Machine-local and sensitive (kept out of the repo)
# nixos/secret.nix: legacy plaintext password hash (agenix replaces it; secrets/*.age ARE committed)
# nixos/local.nix: machine-specific boot entries with disk identifiers
nixos/secret.nix
nixos/local.nix
